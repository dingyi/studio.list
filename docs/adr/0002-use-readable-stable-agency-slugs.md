# Use readable, stable agency slugs

Agency Detail URLs use readable name-based slugs, adding a short Official Domain discriminator only when names collide. Once published, a slug remains stable when display names change so indexed and shared links do not break; explicit redirects are required if a published slug must ever be replaced.
